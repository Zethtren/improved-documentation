apiVersion: v1
kind: Service
metadata:
  name: phoenix-dashboard-svc
  namespace: houston-cv
  labels:
    app: phoenix-dashboard
spec:
  type: ClusterIP
  selector:
    app: phoenix-dashboard
  ports:
    - port: 80
      targetPort: 4000
      protocol: TCP
