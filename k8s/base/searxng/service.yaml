apiVersion: v1
kind: Service
metadata:
  name: searxng-svc
  namespace: houston-cv
  labels:
    app: searxng
spec:
  type: ClusterIP
  selector:
    app: searxng
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
