# WARNING: This secret contains placeholder credentials.
# In production, replace this with SealedSecrets, External Secrets Operator,
# or another secrets management solution. Do NOT commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: surrealdb-credentials
  namespace: houston-cv
  labels:
    app: surrealdb
type: Opaque
data:
  # base64 encoded: "root"
  username: cm9vdA==
  # base64 encoded: "changeme-in-prod"
  password: Y2hhbmdlbWUtaW4tcHJvZA==
  # base64 encoded: "changeme-in-prod" — used by the StatefulSet args via $(SURREAL_PASS)
  SURREAL_PASS: Y2hhbmdlbWUtaW4tcHJvZA==
