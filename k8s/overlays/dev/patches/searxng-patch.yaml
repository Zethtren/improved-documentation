apiVersion: apps/v1
kind: Deployment
metadata:
  name: searxng
  namespace: houston-cv
spec:
  template:
    spec:
      containers:
        - name: searxng
          resources:
            limits:
              memory: "256Mi"
              cpu: "250m"
            requests:
              memory: "128Mi"
              cpu: "100m"
